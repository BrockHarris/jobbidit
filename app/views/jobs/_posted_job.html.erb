<tr>
  <td><%= job.category %></td>
  <td><%= link_to job.title, job %></td> 
  <td>
  	<% if @job.user = current_user %>
			<%= link_to "Edit job", edit_job_path(@job) %> - <%= link_to "Delete", @job, :method => :delete, :class=> :destroy %>
		<% end %>
	</td>
<tr>