<%= form_for @jobtype, :html=> { :multipart => true} do |f| %>
  <%= render 'shared/error_messages', :object => f.object %>
  <%= f.text_field :name, :placeholder => "New job category" %><br/>
  <%= f.submit 'Add' %>
<% end %>